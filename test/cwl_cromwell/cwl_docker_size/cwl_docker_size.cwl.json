{
    "id": "#main",
    "class": "CommandLineTool",
    "hints": [
        {
            "dockerPull": "debian:stretch-slim",
            "class": "DockerRequirement"
        },
        {
            "loadListing": "deep_listing",
            "class": "LoadListingRequirement"
        },
        {
            "networkAccess": true,
            "class": "NetworkAccess"
        }
    ],
    "inputs": [
        {
            "id": "#main/INPUT",
            "type": {
                "type": "array",
                "items": "File"
            }
        }
    ],
    "stdout": "stdout",
    "arguments": [
        {
            "valueFrom": "${\n  if (inputs.INPUT.length == 0) {\n    var cmd = ['echo', \"no inputs\"];\n    return cmd\n  }\n  else {\n    var cmd = [\"echo\", \"execute\"];\n    var use_input = [];\n    for (var i = 0; i < inputs.INPUT.length; i++) {\n      var filesize = inputs.INPUT[i].size;\n      use_input.push(\"order=\".concat(filesize));\n    }\n\n    var run_cmd = cmd.concat(use_input);\n    return run_cmd\n  }\n\n}\n"
        }
    ],
    "out": "file:///Users/yshi/Documents/dnanexus/dxCompiler/test/cwl_cromwell/cwl_docker_size/cwl_docker_size.cwl#stdout",
    "baseCommand": [],
    "outputs": [
        {
            "type": "string",
            "outputBinding": {
                "glob": "stdout",
                "loadContents": true,
                "outputEval": "$(self[0].contents.trim())"
            },
            "id": "#main/stdout_output"
        }
    ],
    "cwlVersion": "v1.2"
}